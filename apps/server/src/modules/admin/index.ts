/**
 * Admin module — Phase 7.
 *
 * Will own: admin-only routes for users, properties, CRM, email campaigns,
 * ROI assumptions, hidden cost rules, area management, prompt editor,
 * system/audit logs, settings. Guarded by `role=admin`.
 */
export {};
