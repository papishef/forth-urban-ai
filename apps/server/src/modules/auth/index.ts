/**
 * Auth module — Phase 1 (see docs/IMPLEMENTATION_PLAN.md#phase-1--foundation-auth-data-layer-design-system).
 *
 * Will own: register, login, JWT access/refresh issuance, email OTP via Resend,
 * Google OAuth (Passport), logout/session revocation, password reset.
 */
export {};
