/**
 * Notifications module — Phase 6.
 *
 * Will own: Resend email sending, wa.me click-to-chat link generation
 * (no Meta Cloud API), in-app `notifications` model, browser notifications.
 */
export {};
