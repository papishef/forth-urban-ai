/**
 * Mongoose connection module — Phase 1.
 *
 * Will own: connection bootstrap with retry/backoff, and base schema
 * plugin adding createdAt/updatedAt/deletedAt/version to every model
 * (see docs/ARCHITECTURE.md#5-database-schema-mongodb-atlas).
 */
export {};
